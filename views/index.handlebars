  
<h1>Eat The Burger!</h1>

<h2>Make The Burger, Devour The Burger!</h2>

<ul>
  {{#each burger}}
    {{#unless devour}}
      {{> burger/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

{{!-- <h2>Cats that are sleepy!</h2> --}}

<ul>
  {{#each burger}}
    {{#if devour}}
      {{> burger/burger-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Cat</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burgers:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Sleepy?</label><br>
    <input type="radio" name="devoured" value="1" checked> Make Burger!<br>
    <input type="radio" name="devoured" value="0"> Devour Burger!
  </div>

  <button type="submit">Add Burger</button>
</form>